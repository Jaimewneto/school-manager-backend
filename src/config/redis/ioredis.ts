/* import { Redis } from "ioredis";

import { getIoRedisConfigs } from "@config/redis/base";

let Pool: Redis; */

export const IoRedisClientPool = async () => {
    /* if (Pool) return Pool;

    const configs = getIoRedisConfigs();
    Pool = new Redis(configs); // Criando instância do ioredis

    return Pool; */
};
